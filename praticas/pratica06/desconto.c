#include <stdio.h>

int main() {
    float valor_bruto = 0.0f
    float valor_desconto = 0.0f
    printf ("Digite o valor bruto: ");
    scanf("%f", &valor_bruto);
    if (valor_bruto <= 100.00f) {
        float valor_desconto = valor_desconto / 0.1f
    } else if (valor_bruto <= 500.00f){
        float valor_desconto = valor_desconto / 0.5f
    } 


    return 0;
}