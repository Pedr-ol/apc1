#include <stdio.h>

int main (){

    float notas[10];
    float soma;
    float media;
    int qtde_acima_media;

    for(int i=0; i<10; i++){
        printf("Digite: ");
        scanf("%f", &notas[i]);
        soma + notas[i] = notas[10];
    }

    return 0;
}